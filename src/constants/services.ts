/**
 * Slug estable de la categoría raíz de servicios veterinarios.
 * Usado por el auto-seed (entry.ts) y por la vista de servicios.
 */
export const ROOT_SERVICE_CATEGORY_SLUG = '_vet-services-root';
